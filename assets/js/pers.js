function contactInfo(){
    
    window.scrollBy({
        top: 3500,  // Number of pixels to scroll by
        left: 0,
        behavior: 'smooth'  // Smooth scrolling
    });
}

function MoreInfo(){
    window.scrollBy({
        top: 800,  // Number of pixels to scroll by
        left: 0,
        behavior: 'smooth'  // Smooth scrolling
    });
}

function Experience(){
    window.scrollBy({
        top: 1400,  // Number of pixels to scroll by
        left: 0,
        behavior: 'smooth'  // Smooth scrolling
    });
}

function Projects(){
    window.scrollBy({
        top: 1900,  // Number of pixels to scroll by
        left: 0,
        behavior: 'smooth'  // Smooth scrolling
    });
}